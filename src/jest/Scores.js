/* eslint no-undef: 0  react/jsx-filename-extension: 0 */
import React from 'react';
import { render } from '@testing-library/react';

test('Should win: case player achieved 4 colors', () => {
    expect(sum(1, 2)).toBe(3);
});

test('Game Should finish: case player used all ask times', () => {
    expect(sum(1, 2)).toBe(3);
});

test('Should reset: case Reset trigged', () => {
    expect(sum(1, 2)).toBe(3);
});

test('Should Open: case Ansform trigged', () => {
    expect(sum(1, 2)).toBe(3);
});

test('Should Open: case Askform trigged', () => {
    expect(sum(1, 2)).toBe(3);
});

test('Should Open: case Setupform trigged', () => {
    expect(sum(1, 2)).toBe(3);
});

test('Should Open: case Secretcodesetup trigged', () => {
    expect(sum(1, 2)).toBe(3);
});

test('Asktable Should Resize: case Window width is less or equal 1024px', () => {
    expect(sum(1, 2)).toBe(3);
});

test('Anstable Should Resize: case Window width is less or equal 1024px', () => {
    expect(sum(1, 2)).toBe(3);
});
